<%= form_for(@scanned) do |f| %>
  <% if @scanned.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scanned.errors.count, "error") %> prohibited this scanned from being saved:</h2>

      <ul>
      <% @scanned.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
